<div class="container">
  <div class="row">
  <% @artists.each do |artist| %>
    <div class='col-xs-12 col-sm-6 col-md-3'>
      <div class="card box-shadow" style='padding-bottom: 20px;'>
        <img class="img-responsive" src="<%= artist.image_url %>" alt="<%= artist.name %>" />
        <h3 class="text-center"><%= artist.name %></h3>
        <%= link_to "Ver Más", artist_path(artist), class: "btn btn-md btn-block btn-primary" %>
      </div>
    </div>
  <% end %> 
  </div>
</div>